Make=$(MAKE) -s

$(Out)/%.html : %(In)/%.md
	pandoc -o $@  $<

$(Out)/% : $(In)/%
	(echo 111; cat $<) >  $@

test:
	$(Make) In=$(PWD) Out=$(PWD) debug 

files=$(shell ls * | awk '{print "'$(OUT)'/" $$1}')

debug:
	echo $(files)
